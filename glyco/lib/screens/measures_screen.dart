import 'package:flutter/material.dart';

class MeasuresScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Measures Screen"),
    );
  }
}
